kettleandcloud-trends

Public view of the weekly trend digest. `index.html` is generated by
`trend-tools/` (see that folder's README); to redeploy a fresh run:

    cp ../digests/trends-dashboard.html index.html
    git commit -am "refresh digest" && git push
